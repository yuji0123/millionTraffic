<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :userId %><br>
    <%= f.text_field :userId %>
  </div>
  <div class="field">
    <%= f.label :userCompany %><br>
    <%= f.text_area :userCompany %>
  </div>
  <div class="field">
    <%= f.label :userDiscountRate %><br>
    <%= f.number_field :userDiscountRate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
